# Context-specific words (REQ-025; ASVS 5.0 6.1.2 and 6.2.11).
#
# The documented list of words a password may not be built on because they are about *this*
# system: the product, the ecosystem it belongs to, and the words on its own sign-in screen.
# Matching is by containment for these, after lower-casing, so "D3Auth-password-2026" is refused.
# The instance's own names — its operator display name and issuer host — are added at boot.
#
# The breached-password corpus is separate: breached-passwords.txt.gz (see policy.ts).
#
# One entry per line, lower case, '#' starts a comment.
d3auth
d3 auth
d3-auth
d3cloud
d3 cloud
d3-cloud
demers
bindery
openid
passkey
authenticator
