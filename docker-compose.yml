# Base stack: postgres + migrate + server, no host ports, no ingress.
#   Local dev:  pnpm dev:up   (docker-compose.dev.yml)
#   Zima:       docker compose -f docker-compose.yml -f docker-compose.tunnel.yml up -d
x-server-hardening: &hardening
  read_only: true
  tmpfs:
    - /tmp
  cap_drop: [ALL]
  security_opt:
    - no-new-privileges:true

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: d3auth
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-d3auth}
      POSTGRES_DB: d3auth
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U d3auth -d d3auth"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  # Interim until T-1.10 moves migrations into boot behind a pre-migration dump.
  migrate:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    image: ghcr.io/d3cloud/d3auth:local
    command: ["node_modules/.bin/prisma", "migrate", "deploy"]
    environment:
      DATABASE_URL: postgresql://d3auth:${POSTGRES_PASSWORD:-d3auth}@postgres:5432/d3auth
    depends_on:
      postgres:
        condition: service_healthy
    <<: *hardening
    restart: "no"

  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    image: ghcr.io/d3cloud/d3auth:local
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql://d3auth:${POSTGRES_PASSWORD:-d3auth}@postgres:5432/d3auth
    depends_on:
      migrate:
        condition: service_completed_successfully
    volumes:
      - backups:/backups
      # Seed file mount returns with T-4.6; a missing bind source would be created as a directory.
    # Healthcheck (/readyz) is defined in the image.
    <<: *hardening
    restart: unless-stopped

volumes:
  pgdata:
  backups:
