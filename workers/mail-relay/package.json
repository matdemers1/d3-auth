{
  "name": "d3auth-mail-relay",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker: POST /send via send_email",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
